# Q-SYS Formular Projekt

Ein einfaches HTML-Formular zur Steuerung von UCI TextControls auf einem Q-SYS Core 110f über WebSocket und QRWC.

## Setup

1. Öffne `index.html` in einem Browser.
2. Stelle sicher, dass dein Q-SYS Core über WebSocket erreichbar ist.
3. Passe die IP-Adresse im Script an.
